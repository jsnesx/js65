{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "js65 project file",
	"description": "Compilation units the js65 language server assembles. Paths resolve against this file's directory.",
	"type": "object",
	"required": ["units"],
	"properties": {
		"units": {
			"type": "array",
			"description": "One entry per independently-assembled program.",
			"items": {
				"type": "object",
				"required": ["sources"],
				"properties": {
					"name": {
						"type": "string",
						"description": "Human-readable unit name. Defaults to the first source's basename."
					},
					"sources": {
						"type": "array",
						"description": "Entry source files assembled for this unit.",
						"items": { "type": "string" }
					},
					"includePaths": {
						"type": "array",
						"description": "Search path for `.include`.",
						"items": { "type": "string" }
					},
					"binIncludePaths": {
						"type": "array",
						"description": "Search path for `.incbin`.",
						"items": { "type": "string" }
					},
					"linkerConfig": {
						"type": "string",
						"description": "Path to an ld65 config file. Linking runs on save to surface segment-overflow errors."
					},
					"target": {
						"type": "string",
						"description": "Built-in target layout, used when no linkerConfig is given.",
						"examples": ["nes", "none"]
					}
				},
				"additionalProperties": false
			}
		},
		"lint": {
			"type": "object",
			"description": "Lint configuration for every unit in this project. Lints run by default; a single line can also be silenced with a `; js65-lint-disable-next-line RULE` comment.",
			"properties": {
				"enabled": {
					"type": "boolean",
					"description": "Set false to turn off every lint rule in this project.",
					"default": true
				},
				"rules": {
					"type": "object",
					"description": "Level per rule. A rule not listed here keeps its default level.",
					"properties": {
						"bare-number-operand": {
							"type": "string",
							"enum": ["off", "info", "warning"],
							"description": "A lone decimal/binary literal used as an address, e.g. `lda 5`. Default: warning."
						},
						"suspicious-address-expr": {
							"type": "string",
							"enum": ["off", "info", "warning"],
							"description": "A lo/hi byte expression used as an address, e.g. `lda <label`. Default: warning."
						},
						"endproc-no-terminator": {
							"type": "string",
							"enum": ["off", "info", "warning"],
							"description": "`.endproc` whose last instruction falls through. Default: warning."
						},
						"jsr-rts-tail-call": {
							"type": "string",
							"enum": ["off", "info", "warning"],
							"description": "`jsr` immediately followed by `rts`, which could be a `jmp`. Default: info."
						},
						"jmp-fallthrough": {
							"type": "string",
							"enum": ["off", "info", "warning"],
							"description": "`jmp` to the label defined on the next line. Default: info."
						}
					},
					"additionalProperties": false
				}
			},
			"additionalProperties": false
		}
	},
	"additionalProperties": false
}
